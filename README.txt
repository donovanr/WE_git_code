To use this code:

- cd to the src directory and issue make to compile the source
- run WE using the runWE.sh script.  there are a few example systems included in the input folder.
  - example: ./runWE.sh input/schlogl.input schlogl.snapshot